{
  "name": "floatcursor-builder",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "crx3": "^1.1.3"
  },
  "scripts": {
    "build:crx": "crx3 src/manifest.json --crx dist/floatcursor.crx --zip dist/floatcursor.zip --key key.pem"
  }
}
